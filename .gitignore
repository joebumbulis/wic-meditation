# Audio and video are hosted on R2 and YouTube, never in git.
# (Images ARE committed — they're small and ship with the page.)
*.m4a
*.mp3
*.mp4
*.mov
*.wav
*.aac
*.webm

# Source files from photo/design tools — export the web version instead
*.psd
*.ai
*.sketch
*.fig
*.tiff
*.raw
*.cr2
*.arw

# macOS
.DS_Store
._*
.Spotlight-V100
.Trashes

# Editors
.vscode/
.idea/
*.swp

# Node, if a build step ever appears
node_modules/